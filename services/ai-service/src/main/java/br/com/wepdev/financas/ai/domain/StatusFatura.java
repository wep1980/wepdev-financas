package br.com.wepdev.financas.ai.domain;

/** Cópia própria do enum do card-service — sem lib compartilhada entre serviços (ADR-0001). */
public enum StatusFatura {
    ABERTA,
    FECHADA,
    PAGA
}
