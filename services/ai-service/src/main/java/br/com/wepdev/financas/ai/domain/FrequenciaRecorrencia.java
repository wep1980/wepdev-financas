package br.com.wepdev.financas.ai.domain;

/** Só MENSAL no v1 — mesmo enum do transaction-service (ADR-0009), cópia própria (sem lib compartilhada entre serviços). */
public enum FrequenciaRecorrencia {
    MENSAL
}
