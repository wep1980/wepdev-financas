package br.com.wepdev.financas.document.domain;

/**
 * Lista fechada por enquanto (ADR-0004 já prevê extensão futura pra
 * EXTRATO_BANCARIO e BOLETO_FINANCIAMENTO — só entram quando a fatia
 * correspondente for implementada, não adiantar agora).
 */
public enum TipoDocumento {
    FATURA_CARTAO
}
